const QA_PACKAGE='com.knightshield.pentagon.mobiletest';
const QA_CALLBACK_SCHEME='pentagon-customer-test';
const QA_CALLBACK_HOST='login-callback';
const PRIVATE_RECEIVER_PERMISSION_SUFFIX='.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION';
function result(reasons,findings){return{result:reasons.length?'FAIL':'PASS',reason_codes:reasons.length?reasons:['STATIC_POLICY_PASS'],findings};}
function componentByName(facts,name){return facts.components.find(c=>c.name===name);}
export function evaluateAndroidPackageIdentity(facts,releaseClass='android_founder_qa'){
 const reasons=[]; if(releaseClass!=='android_founder_qa')reasons.push('UNSUPPORTED_RELEASE_CLASS'); if(facts.package_name!==QA_PACKAGE)reasons.push('PACKAGE_NAME_MISMATCH'); if(!Number.isInteger(facts.version_code)||facts.version_code<28)reasons.push('VERSION_CODE_INVALID'); if(typeof facts.version_name!=='string'||!/^2\.0\.\d+-test$/.test(facts.version_name))reasons.push('VERSION_NAME_INVALID'); if(!Number.isInteger(facts.target_sdk)||facts.target_sdk<35)reasons.push('TARGET_SDK_TOO_LOW'); if(!Number.isInteger(facts.min_sdk)||facts.min_sdk<24)reasons.push('MIN_SDK_POLICY_MISMATCH'); const main=componentByName(facts,'com.knightshield.pentagon.MainActivity'); if(!main||main.type!=='activity'||main.exported!==true)reasons.push('MAIN_ACTIVITY_IDENTITY_INVALID'); const cb=facts.data.find(x=>x.scheme===QA_CALLBACK_SCHEME&&x.host===QA_CALLBACK_HOST); if(!cb)reasons.push('AUTH_CALLBACK_IDENTITY_INVALID'); return result(reasons,{package_name:facts.package_name,version_code:facts.version_code,version_name:facts.version_name,target_sdk:facts.target_sdk,min_sdk:facts.min_sdk,main_activity:main??null,auth_callback_present:Boolean(cb)});
}
export function evaluateAndroidSecurityStatic(facts,releaseClass='android_founder_qa'){
 const reasons=[]; if(releaseClass!=='android_founder_qa')reasons.push('UNSUPPORTED_RELEASE_CLASS'); const expected=`${QA_PACKAGE}${PRIVATE_RECEIVER_PERMISSION_SUFFIX}`; const allowed=new Set(['android.permission.INTERNET',expected]); const unexpected=facts.uses_permissions.filter(p=>!allowed.has(p)); if(unexpected.length)reasons.push('UNEXPECTED_PERMISSION'); if(!facts.uses_permissions.includes('android.permission.INTERNET'))reasons.push('INTERNET_PERMISSION_MISSING'); if(facts.application.usesCleartextTraffic!==false)reasons.push('CLEARTEXT_DENY_NOT_EXPLICIT'); const unsafe=[]; for(const c of facts.components){if(c.exported!==true)continue;const isMain=c.type==='activity'&&c.name==='com.knightshield.pentagon.MainActivity';const isProfile=c.type==='receiver'&&c.name==='androidx.profileinstaller.ProfileInstallReceiver'&&c.permission==='android.permission.DUMP';if(!isMain&&!isProfile)unsafe.push(`${c.type}:${c.name??'unknown'}`);} if(unsafe.length)reasons.push('UNSAFE_EXPORTED_COMPONENT'); const providers=facts.components.filter(c=>c.type==='provider'&&c.exported===true);if(providers.length)reasons.push('EXPORTED_PROVIDER'); return result(reasons,{permissions:facts.uses_permissions,unexpected_permissions:unexpected,uses_cleartext_traffic:facts.application.usesCleartextTraffic??null,unsafe_exported_components:unsafe,exported_provider_count:providers.length});
}
export function evaluateAndroidPrivacyStatic(facts,releaseClass='android_founder_qa'){
 const reasons=[]; if(releaseClass!=='android_founder_qa')reasons.push('UNSUPPORTED_RELEASE_CLASS'); if(facts.application.allowBackup!==false)reasons.push('BACKUP_NOT_DISABLED'); if(typeof facts.application.dataExtractionRules!=='string'||!facts.application.dataExtractionRules.startsWith('@0x'))reasons.push('DATA_EXTRACTION_RULES_MISSING'); if(typeof facts.application.fullBackupContent!=='string'||!facts.application.fullBackupContent.startsWith('@0x'))reasons.push('FULL_BACKUP_RULES_MISSING'); const prefixes=['android.permission.CAMERA','android.permission.RECORD_AUDIO','android.permission.ACCESS_FINE_LOCATION','android.permission.ACCESS_COARSE_LOCATION','android.permission.READ_CONTACTS','android.permission.WRITE_CONTACTS','android.permission.READ_EXTERNAL_STORAGE','android.permission.WRITE_EXTERNAL_STORAGE','android.permission.READ_MEDIA_']; const sensitive=facts.uses_permissions.filter(p=>prefixes.some(x=>p.startsWith(x))); if(sensitive.length)reasons.push('UNNECESSARY_SENSITIVE_PERMISSION'); return result(reasons,{allow_backup:facts.application.allowBackup??null,data_extraction_rules:facts.application.dataExtractionRules??null,full_backup_content:facts.application.fullBackupContent??null,sensitive_permissions:sensitive});
}
